/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['localhost'], // Autoriser les images provenant de localhost
  },
  // Ajoute d'autres configurations si nécessaire
}

export default nextConfig;

